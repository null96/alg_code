[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "alg-code"
version = "0.1.0"
description = "Minimal runnable project for OpenClaw local-execution validation."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["automl_runner"]
